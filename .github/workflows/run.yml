name: Run Subtitle Downloader

on:
  schedule:
    - cron: "*/15 * * * *"  # Every 15 minutes
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: subtitle-downloader
  cancel-in-progress: true   # Job mới sẽ hủy job cũ nếu còn chạy

jobs:
  run-script:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run script
      run: python run.py

    - name: Commit and push changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add vtsubtitle/ ytsubtitle/ progress.json
        git diff --cached --quiet || git commit -m "chore: Update subtitles and progress [$(date -u '+%Y-%m-%d %H:%M UTC')]"
        git push